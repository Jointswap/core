
// const { BN, bufferToHex, privateToAddress, toBuffer } = require("ethereumjs-util")

//     module.exports = async function (taskArguments, hre, runSuper) {

//         await
//     .setAction(async function ({ ethers: { getNamedSigner } }) {
        
//             const premint = taskArgs.mintamount * 10 ** decimals()

//             const tokenContract = await ethers.getContract("GeneralToken");

//             const deployToken = await tokenContract.deploy(taskArgs.name, taskArgs.symbol, premint)

//             console.log("Token deployed to ", deployToken.address)
        

//         });
//     });
// };

    
